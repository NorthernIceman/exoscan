{
    "control_name": "compute_instance_pool_instances_different_template",
    "control_description": "Check if any instance pool has instances that have a different os template than conigured in the pool config.",
    "threat": "If some instances in an instance pool are not running the specified OS template, unpatched instances, deprecated services or incompatibility can be the consequence.",
    "recommendation": "Make sure to delete instances with differing os templates so the pool can run on its desired capacity again and no incompatibilities arise.",
    "severity": "high"
}